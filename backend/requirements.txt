fastapi==0.115.0
uvicorn[standard]==0.30.0
supabase==2.28.3
python-dotenv==1.0.1
apscheduler==3.10.4
pydantic==2.13.0
httpx==0.27.0          # 預留：未來呼叫影像辨識模組外部 API 時使用（TODO ❹）
pywebpush==1.14.0       # Web Push 整合所需依賴
py-vapid>=1.7.0         # 產生 VAPID 密鑰對所需依賴
cryptography>=42.0.0    # 加密相關依賴

